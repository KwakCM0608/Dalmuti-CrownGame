<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ always owns the first launch frame.  Give that system
         surface the same crown, colours, and wordmark as the former web
         splash so the hand-off reads as one continuous DALMUTI splash. -->
    <style name="DalmutiLaunchTheme" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/backgroundColor</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="android:windowSplashScreenBrandingImage">@drawable/dalmuti_splash_branding</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
